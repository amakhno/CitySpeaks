@model CitySpeaks.Domain.Models.Review

@{
    ViewBag.Title = "Редактирование отзыва";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

@using (Html.BeginForm("Edit", "Review", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <p>
        @if (TempData["message"] != null)
        {
            @TempData["message"]
        }
        @Html.ValidationSummary()
    </p>
    <div class="panel-body">
        @Html.HiddenFor(m => m.Id)
        <div class="form-group">
            @Html.LabelFor(x => x.Name)

            @Html.TextBoxFor(x => x.Name,
                        new { @class = "form-control" })
            @Html.LabelFor(x => x.ShortDescription)
            @Html.TextBoxFor(x => x.ShortDescription,
                        new { @class = "form-control" })
        </div>

        <label>Изображение</label>
        <p><input type="file" name="image1" size="40" id="SmallImage"></p>
        @if (Model == null || Model.Image?.ImageData == null)
        {
            <div class="form-control-static">Нет картинки</div>
        }
        else
        {
            <span class="image">
                <img width="288" height="174"
                     src="@Url.Action("GetImage", "Review",
                        new { Model.Id })" />
            </span>
        }
    </div>

    <ul class="actions">
        <li><input type="submit" value="Сохранить" class="button big" /></li>
    </ul>
}

