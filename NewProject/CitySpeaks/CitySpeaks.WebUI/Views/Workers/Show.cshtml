@model Worker

@{
    ViewBag.Title = Model.Name;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<span class="image main"><img src="@Url.Action("GetBigImage", "Workers",
                        new { workerId = Model.Id })" alt="" /></span>

@Html.Raw(Model.FullDescription)